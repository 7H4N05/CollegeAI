version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - ENVIRONMENT=production
      - SECRET_KEY=collegeai_hackathon_super_secret_jwt_key_2026
    volumes:
      - ./backend:/app
    restart: always

  # Reserved for Frontend integration (Member 2)
  # frontend:
  #   build:
  #     context: ./frontend
  #   ports:
  #     - "3000:3000"

  # Reserved for PostgreSQL database upgrade
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: collegeai
  #     POSTGRES_USER: collegeai
  #     POSTGRES_PASSWORD: secretpassword
  #   ports:
  #     - "5432:5432"
